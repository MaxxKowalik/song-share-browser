{{#each songs as |song|}}


  <section data-id="{{song.id}}">
    <h3>
      {{song.id}}.
      {{song.name}},
      {{song.artist}},
      {{song.album}}:
      {{song.rating}}
      Stars
    <button id='genre-table'>Genre Table</button>
  </h3>

  </section>
{{/each}}
